// const dotenv = require('gulp-dotenv');
// const rename = require('gulp-rename');
// const gulp = require('gulp');

// gulp.task('dotenv', () => gulp.src('.env')
//   .pipe(dotenv())
//   .pipe(rename('env.json'))
//   .pipe(gulp.dest('dist')));

// module.exports = dotenv;
